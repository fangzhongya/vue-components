import { test, expect } from 'vitest';
import { ComponentsResolver } from '../packages/index';

test('../packages/index', () => {
    console.log(ComponentsResolver);

    //expect(index( )).toBe( );
});
