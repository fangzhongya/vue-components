import { test, expect } from 'vitest';
import { ComponentsResolver } from '../packages/resolver';

test('../packages/resolver', () => {
    console.log(ComponentsResolver);

    //expect(resolver( )).toBe( );
});
