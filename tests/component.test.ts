import { test, expect } from 'vitest';
import FangComponent from '../packages/component';

test('../packages/component', () => {
    console.log(FangComponent);

    //expect(component( )).toBe( );
});
